#include <vector>
#include <list>

using namespace std;

list<int> split(list<int> &toSort){

    int mid = toSort.size()/2;

    list<int> left = 
    list<int> right = 

    list<int> sortLeft = split();
    list<int> sortRight = split();

    return toSort;
}

list<int> merge(list<int> &toSort){



    return toSort;
}

vector<int> mergesort_me(vector<int> &toSort){


    // Merge sort
    list<int> lst;
    for(int i : toSort){
        lst.push_back(i);
    }

    split(lst);
    

    return toSort;
}


